<%= form_for @portfolio do |f| %>
  <strong>Portfolio:</strong>
  <%= f.label :name %>
  <%= f.text_field :name %>

  <strong>Coin:</strong>
  <%= f.collection_check_boxes(:coin_ids, Coin.all, :id, :name) %>
  <%= f.submit 'Continue' %>
<% end %>